{% extends 'base.html.twig' %}

{% block title %}Nos bateaux - Entre 2 Rives{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link
        rel="stylesheet"
        href="{{ asset('css/boat-catalogue.css') }}"
    >
{% endblock %}

{% block body %}
    <div class="container boat-catalogue">
        <div class="text-center">
            <h1>Nos bateaux</h1>

            <p class="boat-catalogue-description">
                Choisissez le modèle adapté à votre balade.
            </p>
        </div>

        <form id="boat-search-form" class="boat-search-form">
            <fieldset class="boat-search-form-content">
                <legend class="boat-search-title">
                    Recherche par date et nombre de passagers
                </legend>

                <div class="row g-4 align-items-end">
                    <div class="col-12 col-md-6 col-lg">
                        <label
                            for="search-date"
                            class="form-label"
                        >
                            Date de location
                        </label>

                        <input
                            type="date"
                            id="search-date"
                            name="date"
                            class="form-control"
                            required
                        >
                    </div>

                    <div class="col-12 col-md-6 col-lg">
                        <label
                            for="search-passenger-count"
                            class="form-label"
                        >
                            Nombre de passagers
                        </label>

                        <input
                            type="number"
                            id="search-passenger-count"
                            name="passengerCount"
                            class="form-control"
                            min="1"
                            required
                        >
                    </div>

                    <div class="col-12 col-lg-auto">
                        <button
                            type="submit"
                            class="btn btn-primary-custom"
                        >
                            Rechercher
                        </button>
                    </div>

                    <div class="col-12 col-lg-auto">
                        <button
                            type="reset"
                            class="btn btn-secondary-custom btn-lg-custom"
                            id="boat-search-reset"
                        >
                            Effacer
                        </button>
                    </div>
                </div>
            </fieldset>
        </form>

        <div
            id="boat-results"
            class="row g-5"
            aria-live="polite"
        >
            {% for boatModel in boatModels %}
                <div class="col-12 col-lg-4">
                    <article class="card h-100 shadow-sm">
                        <img
                            src="{{ asset(boatModel.mainImage) }}"
                            alt="Bateau {{ boatModel.name }}"
                            class="card-img-top boat-catalogue-image"
                        >

                        <div class="card-body text-center d-flex flex-column">
                            <h2 class="card-title boat-catalogue-card-title">
                                {{ boatModel.name }}
                            </h2>

                            <p class="boat-catalogue-capacity">
                                Jusqu'à {{ boatModel.capacity }} personnes
                            </p>

                            <p class="card-text">
                                {{ boatModel.description }}
                            </p>

                            <a
                                class="btn btn-primary-custom boat-catalogue-card-button align-self-center"
                                href="{{ path('app_boat_show', {slug: boatModel.slug}) }}"
                                aria-label="Voir le bateau {{ boatModel.name }}"
                            >
                                Voir ce bateau
                            </a>
                        </div>
                    </article>
                </div>
            {% else %}
                <p class="text-center">
                    Aucun bateau n'est disponible actuellement.
                </p>
            {% endfor %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/boat-search.js') }}"></script>
{% endblock %}

